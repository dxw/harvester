- tag = taxonomy_tag(tax)
- case tag[:type]
- when :checkboxes
  - tag[:cls].all.each do |a|
    = check_box_tag('pages[%s][%s][%s]' % [page.id, tax, a.name], '1', page.has_tag?(tax, a.name))
- when :text
  = text_field_tag('pages[%s][%s]' % [page.id, tax], page.tags(tax).join(', '))
